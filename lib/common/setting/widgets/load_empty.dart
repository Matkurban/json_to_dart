import 'package:flutter/material.dart';

class LoadEmpty extends StatelessWidget {
  const LoadEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Icon(Icons.hourglass_empty_outlined));
  }
}
